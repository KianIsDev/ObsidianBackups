#!/bin/bash
cd ~/Documents/Obsidian/ObsidianBackups

while true
do
    git pull
    git add -A
    git commit -a -m "Backup Y"
    git push
    dt=$(date '+%d/%m/%Y');
    echo "$dt"
    sleep 5
done
